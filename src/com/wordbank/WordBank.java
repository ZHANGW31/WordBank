package com.wordbank;

public class WordBank {
    Level currentLevel; // Calling Enum, creating currentLevel instance
    private int questionNumber; // to find out level by determining questionNumber, e.g
    //if questionNumber = 5, level is Easy.

}
